# React + Vite

to start frontend project on local machine

step#01
install node in your machine

step#02
install all required packages in directory of frontend by command
npm i or npm install

step#03
run frontend server at local machine
npm run dev
